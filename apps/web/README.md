# Web App

Next.js frontend for the Ultimate SaaS Boilerplate.

Key points:

- Auth/session state via Zustand
- Data fetching via TanStack Query
- Dashboard for tenant/member/subscription management
- Admin page for super admin user + audit logs

Main env:

```env
NEXT_PUBLIC_API_URL=http://localhost:4000/v1
```
